.icon
    display inline-block
    width 1em
    height 1em
    fill currentColor

{{#shapes}}
.icon-{{base}}
    font-size ({{height.inner}})
    width ({{width.inner}}/{{height.inner}})*1em
{{/shapes}}